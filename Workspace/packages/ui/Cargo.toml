[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true }
{% if is_fullstack -%}
api = { workspace = true }

[features]
server = ["api/server"]
{%- endif %}
